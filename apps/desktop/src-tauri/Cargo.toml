[package]
name = "clone-ai-desktop"
version = "0.1.0"
description = "Local desktop companion for clone-ai"
authors = ["clone-ai contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
url = "2"
